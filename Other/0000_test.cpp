#include <bits/stdc++.h>
using namespace std;

int n,e,u,v;

int main()
{
    ios::sync_with_stdio(0);cin.tie(0);cout.tie(0);
    cin >> n >> e;
    for (int i = 0; i < e; i++)
    {
        cin >> u >> v;
    }
    cout << "hi";
    return 0;
}