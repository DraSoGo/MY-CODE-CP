#include <bits/stdc++.h>
#include <fstream>
using namespace std;

void geninput(string in)
{
    ostream fout;
}

int main()
{
    for (int i = 0; i < 10; i++)
    {
        geninput("./input/input" + to_string(i) + ".txt");
    }
    return 0;
}