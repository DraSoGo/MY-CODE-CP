#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(0);cin.tie(0);cout.tie(0);
    long long n,x;
    cin >> n;
    long long dp[n+1],F[4];
    fill(dp,dp+n+1,INT_MAX);
    for (int i = 0; i < 4; i++)
    {
        cin >> F[i];
    }
    for (int i = 1; i <= n; i++)
    {
    }
    return 0;
}